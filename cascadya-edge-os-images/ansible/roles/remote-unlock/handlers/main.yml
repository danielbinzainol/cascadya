---
- name: Reload systemd manager
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart remote unlock service
  ansible.builtin.systemd_service:
    name: "{{ remote_unlock_systemd_unit_name }}"
    state: restarted
  when: remote_unlock_phase != "bootstrap"
