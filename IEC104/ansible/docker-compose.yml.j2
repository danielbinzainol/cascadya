# Generated by Ansible – do not edit manually
#version: "3.9"

services:

  iec104-bridge:
    build: .
    image: iec104-bridge:latest
    container_name: iec104-bridge
    restart: unless-stopped
    ports:
      - "{{ iec104_port }}:{{ iec104_port }}"
    extra_hosts:
      - "broker-DEV1-S.cascadya.local:51.15.64.139"
    environment:
      IEC104_BIND_IP:    "{{ iec104_bind_ip }}"
      IEC104_PORT:       "{{ iec104_port }}"
      IEC104_CA:         "{{ iec104_ca }}"
      NATS_URL:          "{{ nats_url }}"
      NATS_SUBJECT_PFX:  "{{ nats_subject_pfx }}"
      NATS_TLS_ENABLED:  "{{ nats_tls_enabled }}"
      NATS_TLS_CA:       "{{ nats_tls_ca }}"
      NATS_TLS_CERT:     "{{ nats_tls_cert }}"
      NATS_TLS_KEY:      "{{ nats_tls_key }}"
      LOG_LEVEL:         "{{ log_level }}"
{% if nats_tls_enabled == "true" %}
    volumes:
      - ./certs:/certs:ro
{% endif %}