- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart cascadya-features
  ansible.builtin.systemd:
    name: "{{ features_service_name }}.service"
    state: restarted

