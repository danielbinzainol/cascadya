Run the script by using
```
uv run main.py
```

Data path overrides (useful for Docker/VM):
```
# Global data root override
C_MARKET_DATA_ROOT=/app/data

# Optional project-specific override (example for inariz)
INARIZ_DATA_ROOT=/app/data
```
